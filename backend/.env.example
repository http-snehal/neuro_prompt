# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Get your MongoDB connection string from https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/?appName=YOUR_APP

# JWT Configuration
# Generate a random secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here

# Google Gemini API Configuration
# Get your API keys from https://aistudio.google.com/apikey
# Supports up to 3 keys - if one hits quota, automatically falls back to next
GEMINI_API_KEY=your-primary-api-key-here
GEMINI_API_KEY_2=your-second-api-key-here
GEMINI_API_KEY_3=your-third-api-key-here
